import XCTest

final class CrunchCLISmokeTests: XCTestCase {
    /// Placeholder: the CLI's real end-to-end tests live in the golden-file
    /// suite that will arrive with v0.2. Keeping this target populated so
    /// `swift test` discovers it.
    func testTargetCompiles() {
        XCTAssertTrue(true)
    }
}
